import { ExpenditureCategory } from './expenditure-category';

describe('ExpenditureCategory', () => {
  it('should create an instance', () => {
    expect(new ExpenditureCategory()).toBeTruthy();
  });
});
